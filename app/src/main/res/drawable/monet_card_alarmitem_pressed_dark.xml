<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:left="-5dp" android:right="-5dp">
        <shape android:shape="rectangle">
            <size android:width="48dp" android:height="48dp" />
            <solid android:color="@color/monet_dark_alarm_pressed" />
            <stroke android:width="0dp" android:color="@color/monet_dark_alarm_pressed" />
            <corners android:radius="@dimen/alarmcard_corners_enabled" />
        </shape>
    </item>
</layer-list>