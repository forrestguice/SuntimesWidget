<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="none">


        <item android:id="@+id/addAction"
            android:title="@string/configAction_addAction"
            android:icon="?attr/icActionNew"
            app:showAsAction="never" />

        <item android:id="@+id/deleteAction"
            android:title="@string/configAction_deleteAction"
            android:icon="?attr/icActionDelete"
            app:showAsAction="never" />

        <item android:id="@+id/clearAction"
            android:title="@string/configAction_clearActions"
            android:icon="?attr/icActionDelete"
            app:showAsAction="never" />

        <item android:id="@+id/exportAction"
            android:title="@string/configAction_exportActions"
            android:icon="?attr/icActionSave"
            app:showAsAction="never" />

        <item android:id="@+id/importAction"
            android:title="@string/configAction_importActions"
            android:icon="?attr/icActionCopy"
            app:showAsAction="never" />

    </group>
</menu>
