<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <group>
        <item android:id="@+id/seek_noon"
            android:title="@string/timeMode_noon"
            app:showAsAction="never" />

        <item android:id="@+id/seek_midnight"
            android:title="@string/timeMode_midnight"
            app:showAsAction="never" />
    </group>

    <group>
        <item android:id="@+id/seek_moon_noon"
            android:title="@string/timeMode_moon_noon"
            app:showAsAction="never" />

        <item android:id="@+id/seek_moon_midnight"
            android:title="@string/timeMode_moon_midnight"
            app:showAsAction="never" />
    </group>

</menu>