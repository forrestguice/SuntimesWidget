<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="none">

        <item android:id="@+id/selectAction"
            android:title="@string/actions_action_edit"
            android:icon="?attr/icActionAccept"
            app:showAsAction="always" />

        <item android:id="@+id/editAction"
            android:title="@string/actions_action_edit"
            android:icon="?attr/icActionEdit"
            app:showAsAction="ifRoom" />

        <item android:id="@+id/deleteAction"
            android:title="@string/actions_action_delete"
            android:icon="?attr/icActionDelete"
            app:showAsAction="ifRoom" />

    </group>
</menu>
