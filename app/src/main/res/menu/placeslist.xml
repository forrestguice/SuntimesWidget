<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="none">

        <item android:id="@+id/addPlace"
            android:icon="?attr/icActionNew"
            android:title="@string/configAction_addPlace"
            app:showAsAction="ifRoom"
            android:orderInCategory="0" />

        <item android:id="@+id/clearPlaces"
            android:icon="?attr/icActionDelete"
            android:title="@string/configAction_clearPlaces"
            app:showAsAction="never"
            android:orderInCategory="200" />

        <item android:id="@+id/exportPlaces"
            android:icon="?attr/icActionSave"
            android:title="@string/configAction_exportPlaces"
            app:showAsAction="never"
            android:orderInCategory="300" />

        <item android:id="@+id/addWorldPlaces"
            android:icon="?attr/icActionMap"
            android:title="@string/configLabel_places_build"
            app:showAsAction="never"
            android:orderInCategory="400" />

    </group>
</menu>