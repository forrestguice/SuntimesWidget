<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="none">

        <item android:id="@+id/loadIntent"
            android:title="@string/actions_action_load"
            android:icon="?attr/icActionWidgets"
            app:showAsAction="never" />

        <item android:id="@+id/saveIntent"
            android:title="@string/actions_action_save"
            android:icon="?attr/icActionSave"
            app:showAsAction="never" />

        <item android:id="@+id/testIntent"
            android:title="@string/actions_action_test"
            android:icon="?attr/icActionPlay"
            app:showAsAction="never" />

    </group>
</menu>
