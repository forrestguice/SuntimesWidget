<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="none">

        <item android:id="@+id/action_sort"
            android:icon="?attr/icActionSort"
            android:title="@string/configAction_sortAlarms"
            app:showAsAction="never"
            android:orderInCategory="500">

            <menu>
                <group android:checkableBehavior="single">

                    <item android:id="@+id/sortByAlarmTime"
                        android:title="@string/configAction_sortAlarms_by_time"
                        android:icon="?attr/icActionSort" />

                    <item android:id="@+id/sortByCreation"
                        android:title="@string/configAction_sortAlarms_by_creation"
                        android:icon="?attr/icActionSort" />

                </group>

                <item android:id="@+id/sortEnabledFirst"
                    android:checkable="true" android:checked="false"
                    android:title="@string/configAction_sortAlarms_enabled_first"
                    android:icon="?attr/icActionSort" />
            </menu>

        </item>

        <item android:id="@+id/action_clear"
            android:icon="?attr/icActionDelete"
            android:title="@string/configAction_clearAlarms"
            app:showAsAction="never"
            android:orderInCategory="510" />

        <item android:id="@+id/action_export"
            android:icon="?attr/icActionSave"
            android:title="@string/configAction_exportAlarms"
            app:showAsAction="never"
            android:orderInCategory="520" />

        <item android:id="@+id/action_import"
            android:icon="?attr/icActionCopy"
            android:title="@string/configAction_importAlarms"
            app:showAsAction="never"
            android:orderInCategory="530" />

    </group>
</menu>
